dependencies:
    pre:
        - mkdir build
    override:
        - cd build && cmake ..
        - pwd
        - ls -la


test:
    override:
        - cd build && make -j2 
        - cd build && make -j2 check
