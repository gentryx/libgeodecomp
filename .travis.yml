language: cpp
sudo: false

cache:
  apt: true

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - g++-5
    - clang-3.6

compiler:
    - gcc
    - clang

before_script:
    - mkdir build
    - cd build
    - cmake -DWITH_QT5=false ..

script:
    - make
    - make -j2 tests
    - make check
